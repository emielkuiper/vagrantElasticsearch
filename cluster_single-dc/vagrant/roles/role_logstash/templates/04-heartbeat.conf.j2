input {
  beats {
    port => {{ logstash_listen_port_heartbeat }}
  }
}

output {
  elasticsearch {
    hosts => {{ es_data_hosts }}
    index => "DC1-heartbeat-%{+YYYY.MM.dd}"
    document_type => "%{[@metadata][type]}"
  }
}
